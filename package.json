{
  "name": "lookdebug-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin MCP wrapper for LookinServer and the in-app debug HTTP bridge.",
  "scripts": {
    "build": "node --check src/config.js && node --check src/httpBridgeClient.js && node --check src/lookinClient.js && node --check src/portForwarder.js && node --check src/server.js",
    "start": "node src/server.js"
  }
}
