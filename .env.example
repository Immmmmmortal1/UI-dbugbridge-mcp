# === Bridge 端口说明 ===
# 默认行为：不显式设置 BRIDGE_BASE_URL / BRIDGE_LOCAL_PORT，本地 iproxy 端口动态分配，
# 转发到设备远端 37777（与 Pod 默认值对齐）。同一真机/同一 App 只暴露一个远端端口，
# 连续会话 release 后下个会话直接复用，不依赖多端口隔离。
# - iproxy 模式（旧设备回退）：本地 iproxy 监听动态端口 → 设备端口 37777
# - CoreDevice 隧道模式（iOS 17+ 默认）：直接访问 tunnelIP:37777，忽略 BRIDGE_BASE_URL / BRIDGE_LOCAL_PORT
# 如需固定本地端口（iproxy 模式），可显式开启如下两行示例：
# BRIDGE_BASE_URL=http://127.0.0.1:37777
# BRIDGE_LOCAL_PORT=37777
LOOKIN_CLI_PATH=lookin-cli
LOOKIN_MODE=auto
LOOKIN_HOST=127.0.0.1
# XcodeBuildMCP/CoreDevice identifier (preferred); a physical UDID also works.
LOOKDEBUG_DEVICE_ID=<your-core-device-id-or-udid>
# Legacy compatibility alias:
# LOOKDEBUG_DEVICE_UDID=<your-device-udid>
IPROXY_PATH=iproxy
LOOKIN_LOCAL_PORT=47175
LOOKIN_REMOTE_PORT=47175
# 远端（设备侧）App 监听端口；默认 37777（与 Pod 对齐）；1-65535，否则回退到 37777
# BRIDGE_REMOTE_PORT=37777
LOOKIN_SCREENSHOT_COMMAND=
# 危险开关（默认关闭）：开启后允许 BRIDGE_LOCAL_PORT/BRIDGE_REMOTE_PORT 超出常规范围
# LOOKDEBUG_ALLOW_ANY_PORT=false
# 危险开关（默认关闭）：开启后允许 BRIDGE_BASE_URL 指向非回环主机
# LOOKDEBUG_ALLOW_ANY_URL=false
# 可选：artifact 根目录，设置后 audit_runtime 的输入/输出路径必须位于其内
# LOOKDEBUG_ARTIFACT_ROOT=/path/to/artifacts
